{
  "name": "stampede-app",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile start",
    "build:web": "pnpm --filter web build",
    "lint": "pnpm -r lint",
    "build:web:vercel": "npm --workspace apps/web run build"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
